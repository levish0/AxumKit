# Postgres — DATABASE_URL is what the app/migration read; DB/USER/PASSWORD are consumed
# by the postgres CONTAINER (initdb), not by the app.
DATABASE_URL=postgres://axumkit:axumkit@postgres:5432/axumkit
POSTGRES_DB=axumkit
POSTGRES_USER=axumkit
POSTGRES_PASSWORD=axumkit
