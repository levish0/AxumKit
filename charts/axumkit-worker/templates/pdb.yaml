{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "axumkit-worker.fullname" . }}
  labels:
    {{- include "axumkit-worker.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "axumkit-worker.selectorLabels" . | nindent 6 }}
{{- end }}
