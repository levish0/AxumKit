chore: release v0.7.1

- fix(auth): use append instead of insert for SET_COOKIE header to prevent overwriting
- fix(auth): avoid holding JWKS write lock during network fetch
- update CHANGELOG.md for v0.7.1
